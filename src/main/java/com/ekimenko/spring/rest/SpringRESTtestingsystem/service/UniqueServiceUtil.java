package com.ekimenko.spring.rest.SpringRESTtestingsystem.service;

import java.util.ArrayList;
import java.util.List;

public class UniqueServiceUtil {

    //FIXME implement a unique static method for calling id from model objects
    //FIXME The problem is that for this we need a common class that will extends all the id
/*
    public static <T> List<T> getSet(List<T> modelList , Class<T> tClass) {
        List<Long> listId = new ArrayList<>();
        for (T o : modelList){
            T object = tClass.cast(o);
            object.getId();
            listId.add(id);
        }
    }
*/

}
